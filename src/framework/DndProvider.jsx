// DndProvider.jsx
import React from "react";

/**
 * DndProvider wraps your application and activates the drag-and-drop system.
 * You can add portals, overlays or global listeners here later.
 */
export function DndProvider({ children }) {
  return <>{children}</>;
}
